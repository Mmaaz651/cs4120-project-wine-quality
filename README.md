# CS-4120 Project – Wine Quality

## Setup
1. Create virtual environment: `python -m venv venv`
2. Activate venv and install dependencies: `pip install -r requirements.txt`
3. Run scripts in `src/` folder

## Usage
- `python src/data.py` : Load and preprocess data
- `python src/train_baselines.py` : Train classical ML models
- `python src/train_nn.py` : Train Neural Network models
- `python src/evaluate.py` : Evaluate models and generate plots
